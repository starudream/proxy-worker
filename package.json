{
  "name": "proxy-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "sort:settings": "node scripts/sort-settings.js",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
